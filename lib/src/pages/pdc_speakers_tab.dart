import 'package:flutter/material.dart';

class PDCSpeakersTab extends StatelessWidget {
  const PDCSpeakersTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Welcome to teh speakers tab'));
  }
}
